## URL version

```text
iqSite::init_uv()
  -> site/uv database
  -> qv($uri)
  -> urlVersion::match()
  -> ?qv=<version>
```

## Asset smoke

```text
status + Content-Type
source order
no PHP warning
changed qv
browser fetch
font/image CORS
```
