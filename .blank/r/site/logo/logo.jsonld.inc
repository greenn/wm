<?
$Self = _site::self();
$nH = $Self::nc('header');
$Self::req_css('logo');

$_ctx = $Self::tempCtx(array(
    'src' => ''
));

$ni = $_ctx['src']; 

jsonld::add('logo', array(
	'url' => _i::hostUri($ni),
	'width' => _i::hostUri($ni),
	'height' => _i::hostUri($ni),
	'caption' => 'Логотип организации', // Краткое описание или название изображения, которое может быть названием компании.

	//'representativeOfPage' => true, //Установите true, если изображение является репрезентативным логотипом на странице.
	//'copyrightNotice' => '', //Если применимо, вы можете добавить сведения о копирайте изображения.
	//'uploadDate' => '', //Дата, когда логотип был загружен на сайт.
	//'potentialAction' => '', //Действия, которые пользователь может выполнить с логотипом (например, перейти на домашнюю страницу).
));