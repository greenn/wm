накопитель использования web/js/w файлов


-	-	-	-	понятия
	генерация/компиляция составного wjs-файла (выходной фаил)
		wjs-файл - файл с js-функциией

-	-	-	-
"WJS_URI_PATTERN
"WJS_GEN_URI
"WJS_INFO_PATH

wjs{}
	< stacker - [oo web/php/stacker.man]
		hash
		order
		_stack
		build_conf()
		each_callback_args()

	path() - составлние пути до wjs-файла
	uri() - получение uri-пути до wjs-файла

	genName() - получение компиляционнвй nameId

	getFilesVer() - получение списка иднефицирующих-данных wjs-файлов на данный момент
	getFilesVer_cb()

	getFilesUri() - получение списка uri для используемых wjs-файлов на данный момент
	getFilesUri_cb() - колбек для …

	genContent() - составление содержимого для выходного файла
	genContent_cb() - колбек для получение полных данных об учавствующих wjs-файлов

	genSave()* - сохранение сгенерируемого файла
	genDel()* - удаление сгенерируемого файла
	genProcess() - процедура создания нового компилицонного файла


	@info - инфорация о наличии скомпилированных файлов (+~ Информация)
	infoGet() - получение инфорация о наличии скомпилированных файлов
	infoSave()* - сохранение Информации в файл
	infoDelSame()* - удаление из лок. Информации данных о схожих данных с текущими (требуемыми)
	infoIsSame()*
	infoAdd()* - добавление в Информацию данных о текущей компиляции


	export() - составление результата для текущих wjs-запрсов

-	-	-	-	del
genPath() - получение пути для генерации файла
genUri() - получение uri для комбинированного файла


getFilesPath() - получение путей до учавствующих wjs-файлов
getFilesPath_cb()

getReqInfo() - получить данные о текущем состоянии

//genReqFile_()

-	-	-	-	plan
	получаем id req-данных
	если нетак такого gen файла, то создаём новый
	желательно удалив, предыдущий с таким же списком входящих элементов
			[#s1]
		это сложнее,
			возмодно проще удалять всё что есть, и просто генерить заново
		при разработке, чтобы не удалять постоянно и не копить,
			желателен вариант хранения резульата не в файле, а в сессии
				и каждый раз её обновлять?
		тогда вариант при разработке
			[#r2] иметь вывод списка файлов, а не один, ak export

	[#s1] хранить инфо в отдельном файле
		info.json
		'req-data-hash' => [fn|fn-ctxHash, …]
			проверить схожесть по == value-q && each-value==verify-value

-	-	-	-	dd
	addjs('w/..', -40);
	needjs('w/..:-40');
-	-	-	-	eg
web/test/php/needjs/wjs.php