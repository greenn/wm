strTabMenuParser {

	parse() парсим текст по строке / вычленяем текст, глубину и параметры

	_addrInc() - //увеличить адрес на единицу
	_addrUp() - //Вернуться на адресс выше (по глубине)
	_addrDown() - //уменьшаем адрес на единицу

	_addTo() - //добавить элемент в стек по адресу
		//step: спускаемся внутрь структуры
		//step: присваиваем данные по id в полученый стек
		//step: переходим в элемент

	//fetch()

	- //параметры в строке
		[eg] Рассылки \ name=trg-par link=notify

	optStartSign - (\)
	optItemSep - (пробел);
	optValSep (=)
	sid

	extract() - извлекаем из строки данные
		title
		link
		параметры
		глубина

	_extractOpts() - извлекаем параметры

	getSubListAs() - получить массива определённого свойста из всего списока
	convertToListBy() - q получить массив данных определённого свойста из всего многоуровнего списока