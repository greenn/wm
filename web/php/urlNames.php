<?//1d

function urlNames($fullUrl){

	return array(

	);

	/*

	define('SSL', isset($_SERVER['HTTPS']) ? 's' : '');
	define('HOST', $_SERVER['HTTP_HOST']);
	define('hostName', preg_replace('~^www\.~', '', HOST));
	define('URI', $_SERVER['REQUEST_URI']);
	define('domain', HOST);
	define('hostUrl', 'http'.SSL.'://'.HOST);
	define('domainUrl', hostUrl.'/');
	define('URL', 'http'.SSL.'://'.HOST.URI);
	define('pagePath', strtok(URI, '?'));
	define('pageUrl', strtok(URL, '?')); //?? и никто не юзает
	define('pageUri', trim(pagePath, '/'));

	define('pageUri', ltrim(pagePath, '/'));
	define('pageName', basename(pagePath));
	define('pageQuery', $_SERVER['QUERY_STRING']);
	*/

}